import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#ee1770",
      contrastText: "#000000",
    },
    secondary: {
      main: "#000000",
      contrastText: "#ffffff",
    },
  },
});
